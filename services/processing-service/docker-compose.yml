services:
  postgres:
    build:
      context: ./docker/postgres
      dockerfile: Dockerfile
    container_name: processing-service-postgres
    environment:
      POSTGRES_USER: ${DATABASE_USER:-processing_user}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD:-processing_pass}
      POSTGRES_DB: ${DATABASE_NAME:-processing_db}
      NRIA_LICENSE_KEY: ${NRIA_LICENSE_KEY}
      NRIA_DISPLAY_NAME: processing-service-postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DATABASE_USER:-processing_user}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - processing-service-network

  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: processing-service-rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER:-guest}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD:-guest}
    ports:
      - "5672:5672"
      - "15672:15672"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - processing-service-network

  localstack:
    image: localstack/localstack:latest
    container_name: processing-service-localstack
    environment:
      SERVICES: s3
      DEBUG: 0
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-test}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-test}
      AWS_DEFAULT_REGION: ${AWS_REGION:-us-east-1}
    ports:
      - "4566:4566"
    volumes:
      - localstack_data:/var/lib/localstack
    healthcheck:
      test: ["CMD", "awslocal", "s3", "ls"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - processing-service-network

  localstack-init:
    image: amazon/aws-cli:latest
    container_name: processing-service-localstack-init
    environment:
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-test}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-test}
      AWS_DEFAULT_REGION: ${AWS_REGION:-us-east-1}
    entrypoint: >
      /bin/sh -c "
        echo 'Aguardando LocalStack...' &&
        until aws --endpoint-url=http://localstack:4566 s3 ls 2>/dev/null; do
          sleep 2;
        done &&
        echo 'Criando bucket archlens-diagramas...' &&
        aws --endpoint-url=http://localstack:4566 s3 mb s3://archlens-diagramas --region ${AWS_REGION:-us-east-1} || true &&
        echo 'Bucket criado com sucesso!'
      "
    depends_on:
      localstack:
        condition: service_healthy
    networks:
      - processing-service-network

  migrations:
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
    container_name: processing-service-migrations
    env_file:
      - .env
    command: ["alembic", "upgrade", "head"]
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - processing-service-network

  processing-service:
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
    container_name: processing-service-app
    env_file:
      - .env
    ports:
      - "8001:8001"
    depends_on:
      migrations:
        condition: service_completed_successfully
      rabbitmq:
        condition: service_healthy
      localstack-init:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - processing-service-network

  newrelic-infra:
    image: newrelic/infrastructure:latest
    container_name: processing-service-newrelic-infra
    cap_add:
      - SYS_PTRACE
    pid: host
    privileged: true
    environment:
      NRIA_LICENSE_KEY: ${NRIA_LICENSE_KEY}
      NRIA_DISPLAY_NAME: ${NRIA_DISPLAY_NAME:-processing-service}
    volumes:
      - /:/host:ro
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      processing-service:
        condition: service_healthy
    networks:
      - processing-service-network

volumes:
  postgres_data:
    driver: local
  localstack_data:
    driver: local

networks:
  processing-service-network:
    driver: bridge
